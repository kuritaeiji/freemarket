require "rails_helper"

RSpec.describe UserMailer, type: :mailer do
  let(:user) { create(:user) }

  it('宛先がユーザーのメールアドレス') do
  end

  it('題名がアカウント有効化') do
  end

  it('有効化リンクが存在する') do
    
  end
end
